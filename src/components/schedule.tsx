import { Course, CourseSelection } from "./course";

export class Schedule {
    constructor(params) {

    }

    public static find_sched(crs_list: CourseSelection[]) {
        // TODO: contruct DLX array -> use dlxmatrix to solve -> interpret and return the result
    }
}