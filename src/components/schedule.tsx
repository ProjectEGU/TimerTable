import { Course, CourseSelection } from "./course";
import { DLXMatrix } from "./dlxmatrix"

export class crs_arrange {
    constructor(params) {

    }

    public static find_sched(crs_list: Course[]):CourseSelection[][] {
        // TODO: contruct DLX array -> use dlxmatrix to solve -> interpret and return the result
        
        return [[],[],[],[]];
    }
}